/**
 * Django Frontend Integrations
 * A guide to how to integrate frontend with django.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Signup model module.
 * @module model/Signup
 * @version 1.0.0
 */
class Signup {
    /**
     * Constructs a new <code>Signup</code>.
     * @alias module:model/Signup
     * @param username {String} 
     * @param passwrod {String} 
     */
    constructor(username, passwrod) { 
        
        Signup.initialize(this, username, passwrod);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, username, passwrod) { 
        obj['username'] = username;
        obj['passwrod'] = passwrod;
    }

    /**
     * Constructs a <code>Signup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Signup} obj Optional instance to populate.
     * @return {module:model/Signup} The populated <code>Signup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Signup();

            if (data.hasOwnProperty('username')) {
                obj['username'] = ApiClient.convertToType(data['username'], 'String');
            }
            if (data.hasOwnProperty('email')) {
                obj['email'] = ApiClient.convertToType(data['email'], 'String');
            }
            if (data.hasOwnProperty('passwrod')) {
                obj['passwrod'] = ApiClient.convertToType(data['passwrod'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Signup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Signup</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Signup.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['username'] && !(typeof data['username'] === 'string' || data['username'] instanceof String)) {
            throw new Error("Expected the field `username` to be a primitive type in the JSON string but got " + data['username']);
        }
        // ensure the json data is a string
        if (data['email'] && !(typeof data['email'] === 'string' || data['email'] instanceof String)) {
            throw new Error("Expected the field `email` to be a primitive type in the JSON string but got " + data['email']);
        }
        // ensure the json data is a string
        if (data['passwrod'] && !(typeof data['passwrod'] === 'string' || data['passwrod'] instanceof String)) {
            throw new Error("Expected the field `passwrod` to be a primitive type in the JSON string but got " + data['passwrod']);
        }

        return true;
    }


}

Signup.RequiredProperties = ["username", "passwrod"];

/**
 * @member {String} username
 */
Signup.prototype['username'] = undefined;

/**
 * @member {String} email
 */
Signup.prototype['email'] = undefined;

/**
 * @member {String} passwrod
 */
Signup.prototype['passwrod'] = undefined;






export default Signup;

